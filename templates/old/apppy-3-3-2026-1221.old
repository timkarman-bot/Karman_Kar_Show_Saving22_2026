{% extends "base.html" %}
{% set title = "Attendee Check-In • " ~ show["title"] %}

{% block content %}
<div class="max-w-3xl">
  <div class="text-xs font-semibold text-slate-500">ATTENDEE CHECK-IN</div>
  <h1 class="mt-1 text-3xl font-semibold tracking-tight">{{ show["title"] }}</h1>
  <p class="mt-2 text-slate-600">
    Quick check-in for attendees. Donations are optional.
  </p>

  {% if error %}
    <div class="mt-5 rounded-2xl border border-red-200 bg-red-50 p-4 text-red-900">{{ error }}</div>
  {% endif %}

  <div class="mt-6 rounded-3xl bg-white border border-slate-200 shadow-sm p-6">
    <form method="POST" class="grid gap-6">
      <div class="grid gap-4 md:grid-cols-2">
        <div>
          <label class="text-sm font-semibold">First name *</label>
          <input name="first_name" class="mt-1 w-full rounded-2xl border border-slate-200 px-4 py-3" required>
        </div>
        <div>
          <label class="text-sm font-semibold">Last name *</label>
          <input name="last_name" class="mt-1 w-full rounded-2xl border border-slate-200 px-4 py-3" required>
        </div>

        <div>
          <label class="text-sm font-semibold">Phone (optional)</label>
          <input name="phone" class="mt-1 w-full rounded-2xl border border-slate-200 px-4 py-3">
        </div>
        <div>
          <label class="text-sm font-semibold">Email (optional)</label>
          <input name="email" type="email" class="mt-1 w-full rounded-2xl border border-slate-200 px-4 py-3">
        </div>

        <div>
          <label class="text-sm font-semibold">ZIP (optional)</label>
          <input name="zip" inputmode="numeric" class="mt-1 w-full rounded-2xl border border-slate-200 px-4 py-3">
        </div>
      </div>

      <label class="flex items-start gap-3 rounded-2xl border border-slate-200 bg-slate-50 p-4">
        <input type="checkbox" name="sponsor_opt_in" class="mt-1">
        <div>
          <div class="font-semibold">Send me sponsor offers and specials</div>
          <div class="text-sm text-slate-600">Optional opt-in</div>
        </div>
      </label>

      <label class="flex items-start gap-3 rounded-2xl border border-slate-200 bg-slate-50 p-4">
        <input type="checkbox" name="updates_opt_in" class="mt-1">
        <div>
          <div class="font-semibold">Send me updates about future events</div>
          <div class="text-sm text-slate-600">Optional opt-in</div>
        </div>
      </label>

      <div class="text-xs text-slate-500">
        By selecting these options, you agree Karman Kar Shows & Events may contact you about the event and, if selected,
        share sponsor offers. Msg/data rates may apply. Opt out anytime.
      </div>

      <button class="btn btn-primary w-fit" type="submit">Continue</button>
    </form>
  </div>
</div>
{% endblock %}
