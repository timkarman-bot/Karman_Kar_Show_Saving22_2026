Karman Kar Shows & Events — Cruise In / Voting Disabled Cleanup v6

Files included:
- app.py
- database.py
- templates/admin_shows.html
- templates/admin.html
- templates/register.html
- templates/placeholder_claim.html
- templates/admin_show_mode.html
- templates/vote_qty.html
- templates/voting_closed.html
- templates/show.html

What changed:
1. Added show type "Cruise In" using internal value cruise_in.
2. Manage Shows create/update screens now expose Type of Show.
3. Admin dashboard Show Type now includes Cruise In.
4. Voting Mode now supports "None — voting disabled".
5. Voting Payment Mode now supports "None — no vote payment".
6. Cruise In defaults voting/payment to none if left on default values during create/update.
7. Registration slots remain fully customizable; blank or 0 capacity means no cap.
8. Voting routes now return closed/disabled instead of attempting payment when voting/payment is none.
9. Existing Full and PopUp behavior remains intact.

Verification commands:
python -m py_compile app.py database.py

findstr /n "cruise_in" app.py database.py templates\admin_shows.html templates\admin.html
findstr /n "Voting is not enabled" templates\voting_closed.html templates\vote_qty.html
findstr /n "payment_mode == \"none\"" app.py

Git:
git status --short
git add -A
git commit -m "Add cruise-in show type and voting disabled mode"
git push origin main
